.venv/
__pycache__/
.DS_Store
*.log

# ONNX Runtime can leave this telemetry identifier in the working directory when Piper starts.
:memory:.ses

# Church-specific names; keep it local, not in a public repo.
# (Comments need their own line - gitignore has no trailing-comment syntax.)
glossary.txt
config.json

# The audio-in model needs its own venv: mlx-vlm pulls a newer mlx than the pipeline pins.
.venv-omni
